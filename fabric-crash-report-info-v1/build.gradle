archivesBaseName = "quilted_fabric_crash_report_info_v1"
version = getSubprojectVersion(project)
upstream_version = getSubprojectUpstreamVersion(project)

dependencies {
	modApi(getQslModule("core", "crash_info"))
}
